---
instruction-set:

  - templates:

  - group: MANUAL
    instructions:

        - id:                 MOVE8
          parent:             MOV8rr
          type:               copy

        - id:                 MOVE16
          parent:             MOV16rr
          type:               copy

        - id:                 MOVE32
          parent:             MOV32rr
          type:               copy

        - id:                 MOVE64
          parent:             MOV64rr
          type:               copy

        - id:                 SUBRSP_pseudo
          parent:             SUB64ri8
          operands:
           - imm:             bound
          uses:               [imm]
          defines:            []

        - id:                 ADDRSP_pseudo
          parent:             ADD64ri8
          operands:
           - imm:             bound
          uses:               [imm]
          defines:            []
